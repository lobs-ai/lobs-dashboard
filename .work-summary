Fixed command palette dismissal lag when navigating home. Changed action timing from 0.1s (during animation) to 0.3s (after animation completes) so heavy OverviewView updates don't interfere with smooth dismissal. Updated 10 unit tests to reflect new timing. Build passes successfully.